name: bbs-to-gh-migration (combined)

on:
  workflow_dispatch:
    inputs:
      csv_path:
        description: 'Path to repos CSV file'
        required: true
        default: 'repos.csv'
        type: string
      bbs_base_url:
        description: 'Bitbucket Server base URL (e.g., https://bitbucket.example.com)'
        required: true
        type: string
      max_concurrent:
        description: 'Maximum concurrent migrations (1-5)'
        required: false
        default: '5'
        type: choice
        options: ['1','2','3','4','5']
      approver:
        description: 'migrator/approver github handler'
        required: true
        default: 'e.g. naik123'
        type: string

permissions:
  contents: read
  actions: write
  issues: write   # needed for the issue-based manual approval step

jobs:
  readiness-check:
    name: PR Readiness Check (PR-only)
    runs-on: windows-latest
    timeout-minutes: 30
    environment: "PAT tokens"
    outputs:
      check-passed: ${{ steps.check-result.outputs.passed }}
      has-warnings: ${{ steps.check-result.outputs.has-warnings }}
      open-pr-count: ${{ steps.check-result.outputs.open-pr-count }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Validate inputs & secrets
        shell: pwsh
        env:
          BBS_BASE_URL: ${{ inputs.bbs_base_url }}
          BBS_PAT: ${{ secrets.BBS_PAT }}
          BBS_USERNAME: ${{ secrets.BBS_USERNAME }}
          BBS_PASSWORD: ${{ secrets.BBS_PASSWORD }}
        run: |
          $missing = @()
          if (-not $env:BBS_BASE_URL) { $missing += 'BBS_BASE_URL (workflow input)' }
          if (-not $env:BBS_PAT -and (-not $env:BBS_USERNAME -or -not $env:BBS_PASSWORD)) {
            $missing += 'BBS_PAT or BBS_USERNAME + BBS_PASSWORD (secrets)'
          }
          if ($missing.Count -gt 0) {
            Write-Host "::error::Missing required values: $($missing -join ', ')"
            exit 1
          }

      - name: Run PR Readiness (PR-only)
        id: run-check
        shell: pwsh
        env:
          BBS_PAT: ${{ secrets.BBS_PAT }}
          BBS_USERNAME: ${{ secrets.BBS_USERNAME }}
          BBS_PASSWORD: ${{ secrets.BBS_PASSWORD }}
          BBS_BASE_URL: ${{ inputs.bbs_base_url }}
        run: |
          Write-Host "===== PR Readiness Check (PR-only) =====" -ForegroundColor Cyan
          & .\scripts\0_pr_pipeline_check.ps1 -CsvPath "${{ inputs.csv_path }}"
          $outCsv = Get-ChildItem -Filter "bbs_pipeline_validation_output-*.csv" | Sort-Object LastWriteTime -Descending | Select-Object -First 1
          if (-not $outCsv) {
            Write-Host "::warning::No output CSV produced by the check script."
            echo "PR_ONLY_PASSED=true" >> $env:GITHUB_ENV
            echo "HAS_WARNINGS=true" >> $env:GITHUB_ENV
            echo "OPEN_PR_COUNT=0" >> $env:GITHUB_ENV
            exit 0
          }

          $data = Import-Csv $outCsv.FullName

          # PR-only: treat any repo with open_pr_count > 0 as a WARNING (not a blocker)
          $openPrRepos = $data | Where-Object { [int]($_.open_pr_count) -gt 0 }
          $openPrCount = ($openPrRepos | Measure-Object).Count

          if ($openPrCount -gt 0) {
            Write-Host ("`n⚠️ WARNING: {0} repo(s) have open PRs" -f $openPrCount) -ForegroundColor Yellow
            echo "PR_ONLY_PASSED=true" >> $env:GITHUB_ENV
            echo "HAS_WARNINGS=true" >> $env:GITHUB_ENV
            echo ("OPEN_PR_COUNT={0}" -f $openPrCount) >> $env:GITHUB_ENV
          } else {
            Write-Host "`n✅ No open PRs detected" -ForegroundColor Green
            echo "PR_ONLY_PASSED=true" >> $env:GITHUB_ENV
            echo "HAS_WARNINGS=false" >> $env:GITHUB_ENV
            echo "OPEN_PR_COUNT=0" >> $env:GITHUB_ENV
          }
          Write-Host ("Output CSV: {0}" -f $outCsv.FullName)

      - name: Set Readiness Outputs (PR-only)
        id: check-result
        shell: pwsh
        run: |
          echo "passed=$env:PR_ONLY_PASSED"        >> $env:GITHUB_OUTPUT
          echo "has-warnings=$env:HAS_WARNINGS"    >> $env:GITHUB_OUTPUT
          echo "open-pr-count=$env:OPEN_PR_COUNT"  >> $env:GITHUB_OUTPUT
          if ($env:HAS_WARNINGS -eq 'true') {
            Write-Host "::warning::Open PR(s) found. This is a WARNING, not a blocker."
          } else {
            Write-Host "::notice::PR-only check clean."
          }

      - name: Upload PR check outputs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bbs-pr-check-${{ github.run_number }}
          path: bbs_pipeline_validation_output-*.csv
          retention-days: 30
          if-no-files-found: warn

      - name: Generate PR Check Summary
        if: always()
        shell: pwsh
        run: |
          $open = $env:OPEN_PR_COUNT
          $csvPath = "${{ inputs.csv_path }}"
          $bbsUrl  = "${{ inputs.bbs_base_url }}"
          $outCsv  = Get-ChildItem -Filter "bbs_pipeline_validation_output-*.csv" | Sort-Object LastWriteTime -Descending | Select-Object -First 1

          Add-Content $env:GITHUB_STEP_SUMMARY '# 🔎 PR Readiness Summary (PR-only)'
          Add-Content $env:GITHUB_STEP_SUMMARY ''
          if ($env:HAS_WARNINGS -eq 'true') {
            Add-Content $env:GITHUB_STEP_SUMMARY "## Overall Status: 🟡 Warning(s) detected"
            Add-Content $env:GITHUB_STEP_SUMMARY ("- **Repos with open PRs**: " + $open)
          } else {
            Add-Content $env:GITHUB_STEP_SUMMARY "## Overall Status: 🟢 No warnings"
            Add-Content $env:GITHUB_STEP_SUMMARY "- **Repos with open PRs**: 0"
          }
          Add-Content $env:GITHUB_STEP_SUMMARY ""
          Add-Content $env:GITHUB_STEP_SUMMARY ("**CSV**: ``" + $csvPath + "``")
          Add-Content $env:GITHUB_STEP_SUMMARY ("**Bitbucket Base URL**: " + $bbsUrl)

          if ($outCsv) {
            $data = Import-Csv $outCsv.FullName | Where-Object { [int]($_.open_pr_count) -gt 0 }
            if ($data) {
              Add-Content $env:GITHUB_STEP_SUMMARY ""
              Add-Content $env:GITHUB_STEP_SUMMARY "### 📌 Repositories with Open PRs"
              Add-Content $env:GITHUB_STEP_SUMMARY ""
              Add-Content $env:GITHUB_STEP_SUMMARY "Project Key | Repo | Open PR Count | Default Branch | Latest Commit"
              Add-Content $env:GITHUB_STEP_SUMMARY "---|---|---:|---|---"
              foreach ($r in $data) {
                $defb = if ($r.default_branch) { "`{$($r.default_branch)`}" } else { "_n/a_" }
                $hash = if ($r.latest_commit_id) { ("`{0`}" -f $r.latest_commit_id.Substring(0,[Math]::Min(12,$r.latest_commit_id.Length))) } else { "_n/a_" }
                Add-Content $env:GITHUB_STEP_SUMMARY ("`{0`}|`{1`}|{2}|{3}|{4}" -f $r.project_key, $r.repo_slug, $r.open_pr_count, $defb, $hash)
              }
            }
          }

  # ----------------------------------------------------------------
  # Job 2: Approval gate — ALWAYS runs (issue-based approval)
  # ----------------------------------------------------------------
  approval-gate:
    name: ✅ Manual Approval to Proceed
    runs-on: ubuntu-latest
    needs: [readiness-check]
    steps:
      - name: Context
        run: |
          echo "Approval is required to proceed to migration."
          echo "Open PRs detected: ${{ needs.readiness-check.outputs.open-pr-count }}"
          echo "Warnings present:   ${{ needs.readiness-check.outputs.has-warnings }}"

      - name: Wait for manual approval
        uses: trstringer/manual-approval@v1
        with:
          secret: ${{ github.token }}
          approvers: ${{ inputs.approver }}
          minimum-approvals: 1
          issue-title: "Approve migration to GitHub"
          issue-body: |
            Please review PR readiness summary in the job above.
            Comment **approved** to proceed, or **deny** to stop.
          exclude-workflow-initiator-as-approver: false
          fail-on-denial: true
          polling-interval-seconds: 30

  # ----------------------------------------------------------------
  # Job 3: Migration — unchanged functional logic from 1-migration.yml
  # ----------------------------------------------------------------
  migrate-repositories:
    name: Migrate Repositories (BBS ➜ GH)
    runs-on: windows-latest
    timeout-minutes: 360
    environment: "PAT tokens"
    needs: [approval-gate]
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup GitHub CLI
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          Write-Host "GitHub CLI Version:" -ForegroundColor Cyan
          gh --version
          Write-Host "`nAuthentication Status:" -ForegroundColor Cyan
          gh auth status

      - name: Install BBS2GH Extension
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          Write-Host "Installing gh-bbs2gh extension..." -ForegroundColor Cyan
          gh extension install github/gh-bbs2gh
          Write-Host "`nVerifying installation:" -ForegroundColor Cyan
          gh bbs2gh --version

      - name: Validate CSV File
        shell: pwsh
        run: |
          $csvPath = "${{ inputs.csv_path }}"
      
          if (-not (Test-Path $csvPath)) {
            throw "CSV file not found: $csvPath"
          }
      
          $repos = Import-Csv -Path $csvPath
          if (-not $repos -or $repos.Count -eq 0) {
            throw "CSV is empty: $csvPath"
          }
      
          $requiredColumns = @('project-key','project-name','repo','github_org','github_repo','gh_repo_visibility')
          $missingColumns = $requiredColumns | Where-Object { $_ -notin $repos[0].PSObject.Properties.Name }
          if ($missingColumns) {
            throw "Missing required columns: $($missingColumns -join ', ')"
          }
      
          $badVis = $repos | Where-Object { $_.'gh_repo_visibility' -notmatch '^(public|private|internal)$' }
          if ($badVis) {
            throw "Invalid gh_repo_visibility values found. Use: public|private|internal"
          }
      
          Write-Host "[SUCCESS] CSV validation passed" -ForegroundColor Green

      - name: Run Concurrent Migration
        shell: pwsh
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
          GH_TOKEN: ${{ secrets.GH_PAT }}
          BBS_USERNAME: ${{ secrets.BBS_USERNAME }}
          BBS_PASSWORD: ${{ secrets.BBS_PASSWORD }}
          BBS_BASE_URL: ${{ inputs.bbs_base_url }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          .\scripts\1_migration.ps1 `
            -MaxConcurrent ${{ inputs.max_concurrent }} `
            -CsvPath "${{ inputs.csv_path }}"

      - name: Generate Migration Summary (Detailed)
        if: always()
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Continue'
      
          # Find latest migration output CSV
          $outputCsv = Get-ChildItem -Filter 'repo_migration_output-*.csv' |
                       Sort-Object LastWriteTime -Descending |
                       Select-Object -First 1
      
          # Header
          Add-Content $env:GITHUB_STEP_SUMMARY '# 🚀 Migration Execution Summary (Bitbucket Server → GitHub)'
          Add-Content $env:GITHUB_STEP_SUMMARY ''
      
          if (-not $outputCsv) {
            Add-Content $env:GITHUB_STEP_SUMMARY '⚠️ **No output CSV found** (`repo_migration_output-*.csv`).'
            Add-Content $env:GITHUB_STEP_SUMMARY ''
            Add-Content $env:GITHUB_STEP_SUMMARY '## 📁 Artifacts'
            Add-Content $env:GITHUB_STEP_SUMMARY ('- Migration logs artifact: `migration-logs-{0}`' -f '${{ github.run_id }}')
            Add-Content $env:GITHUB_STEP_SUMMARY '- Verbose logs artifact: `verbose-logs`'
            Add-Content $env:GITHUB_STEP_SUMMARY ('- Output CSV artifact: `migration-output-csv-{0}`' -f '${{ github.run_id }}')
            exit 0
          }
      
          $results = Import-Csv -Path $outputCsv.FullName
      
          $total   = $results.Count
          $success = ($results | Where-Object { $_.Migration_Status -eq 'Success' }).Count
          $failed  = ($results | Where-Object { $_.Migration_Status -eq 'Failure' }).Count
          $pending = ($results | Where-Object { $_.Migration_Status -eq 'Pending' }).Count
          $successRate = if ($total -gt 0) { [math]::Round(($success / $total) * 100, 2) } else { 0 }
      
          # Overall stats
          Add-Content $env:GITHUB_STEP_SUMMARY '## 📊 Overall Statistics'
          Add-Content $env:GITHUB_STEP_SUMMARY ('- **Total Repositories**: {0}' -f $total)
          Add-Content $env:GITHUB_STEP_SUMMARY ('- **Successfully Migrated**: ✅ {0}' -f $success)
          Add-Content $env:GITHUB_STEP_SUMMARY ('- **Failed Migrations**: ❌ {0}' -f $failed)
          Add-Content $env:GITHUB_STEP_SUMMARY ('- **Pending/Not Started**: ⏳ {0}' -f $pending)
          Add-Content $env:GITHUB_STEP_SUMMARY ('- **Success Rate**: {0}%' -f $successRate)
          Add-Content $env:GITHUB_STEP_SUMMARY ('- **Concurrent Jobs**: {0}' -f '${{ inputs.max_concurrent }}')
          Add-Content $env:GITHUB_STEP_SUMMARY ''
      
          # Artifacts
          Add-Content $env:GITHUB_STEP_SUMMARY '## 📁 Artifacts'
          Add-Content $env:GITHUB_STEP_SUMMARY ('- Output CSV: `{0}` (uploaded as `migration-output-csv-{1}`)' -f $outputCsv.Name, '${{ github.run_id }}')
          Add-Content $env:GITHUB_STEP_SUMMARY ('- Migration logs: `migration-logs-{0}` (pattern: `migration-*.txt`)' -f '${{ github.run_id }}')
          Add-Content $env:GITHUB_STEP_SUMMARY '- Verbose logs: `verbose-logs` (patterns: `*.log`, `**/*.log`, `logs/**/*.log`)'
          Add-Content $env:GITHUB_STEP_SUMMARY ''
      
          # Failed table
          if ($failed -gt 0) {
            Add-Content $env:GITHUB_STEP_SUMMARY '## ❌ Failed Migrations'
            Add-Content $env:GITHUB_STEP_SUMMARY ''
            Add-Content $env:GITHUB_STEP_SUMMARY '| Repository | BBS Project | GitHub Org | Log File |'
            Add-Content $env:GITHUB_STEP_SUMMARY '|------------|-------------|------------|----------|'
      
            ($results | Where-Object { $_.Migration_Status -eq 'Failure' }) | ForEach-Object {
              $repoName = if ($_.repo) { $_.repo } else { $_.github_repo }
              $bbsProj  = $_.'project-key'
              $ghOrg    = $_.github_org
              $logFile  = $_.Log_File
              Add-Content $env:GITHUB_STEP_SUMMARY ('| {0} | {1} | {2} | {3} |' -f $repoName, $bbsProj, $ghOrg, $logFile)
            }
      
            Add-Content $env:GITHUB_STEP_SUMMARY ''
          }
      
          # Success table (first 10)
          if ($success -gt 0) {
            Add-Content $env:GITHUB_STEP_SUMMARY '## ✅ Successfully Migrated Repositories'
            Add-Content $env:GITHUB_STEP_SUMMARY ('Total: **{0}** repositories migrated successfully' -f $success)
            Add-Content $env:GITHUB_STEP_SUMMARY ''
            Add-Content $env:GITHUB_STEP_SUMMARY '| Repository | GitHub URL |'
            Add-Content $env:GITHUB_STEP_SUMMARY '|------------|------------|'
      
            ($results | Where-Object { $_.Migration_Status -eq 'Success' } | Select-Object -First 10) | ForEach-Object {
              $repoName = if ($_.repo) { $_.repo } else { $_.github_repo }
              $ghUrl = 'https://github.com/{0}/{1}' -f $_.github_org, $_.github_repo
              Add-Content $env:GITHUB_STEP_SUMMARY ('| {0} | {1} |' -f $repoName, $ghUrl)
            }
      
            $more = $success - 10
            if ($more -gt 0) {
              Add-Content $env:GITHUB_STEP_SUMMARY ''
              Add-Content $env:GITHUB_STEP_SUMMARY ('_... and **{0}** more_' -f $more)
            }
          }
          
      - name: Upload Migration Logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: migration-logs-${{ github.run_id }}
          path: migration-*.txt
          retention-days: 90
          if-no-files-found: warn

      - name: Upload Verbose Logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: verbose-logs
          path: |
            *.log
            **/*.log
            logs/**/*.log
          retention-days: 90
          if-no-files-found: warn

      - name: Upload Output CSV
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: migration-output-csv-${{ github.run_id }}
          path: repo_migration_output-*.csv
          retention-days: 90
          if-no-files-found: error

  # ----------------------------------------------------------------
  # Job 4: Post-migration validation — from 2-migration-validation-bbs.yml
  # ----------------------------------------------------------------
  validate-migrations:
    name: Validate Migrated Repositories (BBS → GH)
    runs-on: windows-latest
    timeout-minutes: 120
    environment: "PAT tokens"
    needs: [migrate-repositories]
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Divert job summary (table-only later)
        shell: pwsh
        run: |
          echo "REAL_SUMMARY=$env:GITHUB_STEP_SUMMARY" >> $env:GITHUB_ENV
          $discard = Join-Path $env:RUNNER_TEMP "_discarded_summary.md"
          echo "GITHUB_STEP_SUMMARY=$discard" >> $env:GITHUB_ENV

      - name: Setup GitHub CLI
        shell: pwsh
        run: |
          gh --version
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

      - name: Resolve inputs to env
        shell: pwsh
        run: |
          echo "CSV_FILE=${{ inputs.csv_path }}" >> $env:GITHUB_ENV
          echo "BBS_BASE_URL=${{ inputs.bbs_base_url }}" >> $env:GITHUB_ENV
          if ("${{ inputs.gh_host }}") { echo "GH_HOST=${{ inputs.gh_host }}" >> $env:GITHUB_ENV }

      - name: Validate Inputs
        shell: pwsh
        run: |
          if (-not $env:CSV_FILE) { throw "csv_path is required" }
          if (-not $env:BBS_BASE_URL) { throw "bbs_base_url is required" }
          Write-Host "CSV_FILE=$env:CSV_FILE"
          Write-Host "BBS_BASE_URL=$env:BBS_BASE_URL"
          if ($env:GH_HOST) { Write-Host "GH_HOST=$env:GH_HOST" }

      - name: Validate CSV File & Columns
        shell: pwsh
        run: |
          $csvPath = $env:CSV_FILE
          if (-not (Test-Path $csvPath)) {
            Write-Error "CSV file not found: $csvPath"; exit 1
          }
          $csv = Import-Csv -Path $csvPath
          if (-not $csv -or $csv.Count -eq 0) {
            Write-Error "CSV appears empty: $csvPath"; exit 1
          }
          $required = @('project-key','repo','url','github_org','github_repo')
          $cols = $csv[0].PSObject.Properties.Name
          $missing = $required | Where-Object { $_ -notin $cols }
          if ($missing.Count -gt 0) {
            Write-Error ("Missing required columns in CSV: {0}" -f ($missing -join ', ')); exit 1
          }
          Write-Host "✅ CSV structure validation passed"
          Write-Host "Total repositories to validate: $($csv.Count)"

      - name: Run Migration Validation (BBS to GH)
        shell: pwsh
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
          GH_TOKEN: ${{ secrets.GH_PAT }}
          GH_HOST: ${{ env.GH_HOST }}
          BBS_TOKEN: ${{ secrets.BBS_TOKEN }}
          BBS_AUTH_TYPE: ${{ secrets.BBS_AUTH_TYPE }}
          BBS_USERNAME: ${{ secrets.BBS_USERNAME }}
          BBS_PASSWORD: ${{ secrets.BBS_PASSWORD }}
        run: |
          .\scripts\2_migration_validation_bbs.ps1 -CsvPath "$env:CSV_FILE" -BbsBaseUrl "$env:BBS_BASE_URL"

      - name: Upload Validation Results (Logs + CSV + MD)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: validation-results-bbs-${{ github.run_number }}
          path: |
            validation-*.txt
            validation-*.json
            validation-summary.csv
            validation-summary.md
          overwrite: true
          retention-days: 30

      - name: Restore real summary & clear
        if: always()
        shell: pwsh
        run: |
          if (-not $env:REAL_SUMMARY) { throw "REAL_SUMMARY not set." }
          $env:GITHUB_STEP_SUMMARY = $env:REAL_SUMMARY
          [System.IO.File]::WriteAllText($env:GITHUB_STEP_SUMMARY, [string]::Empty)

      - name: Append Table Summary (ONLY)
        if: always()
        shell: pwsh
        run: |
          if (Test-Path "validation-summary.md") {
            $content = Get-Content "validation-summary.md" -Raw
            $header = @(
              "## Repository Validation Summary",
              "",
              $content
            ) -join "`n"
            $header | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Encoding utf8 -Append
          } else {
            Write-Host "validation-summary.md not found; skipping summary table."
          }
